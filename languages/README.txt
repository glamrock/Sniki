Basic:
To use all languages, simply replace your current `/languages/` folder with this one.

Advanced:
To use individual languages, add the appropriate files from `/classes/` and `/messages/`.  Then, replace `Names.php` with the copy here.